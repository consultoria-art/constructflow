{
  "name": "constructflow-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma db push --accept-data-loss && npx prisma generate && node server.js",
    "build": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^6.0.0"
  }
}
